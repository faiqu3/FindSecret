id: github2

info:
  name: github2
  author: Faique
  severity: high
  tags: token,file,cloudinary


requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        regex:
          - ghp_.{36}
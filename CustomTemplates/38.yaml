id: sendgrid

info:
  name: js-secret
  author: Faique
  severity: CRITICAL
  tags: token,file,github


requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        regex:
          - "\b(npm_[A-Za-z0-9]{36})\b"
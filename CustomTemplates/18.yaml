id: facebook

info:
  name: js-secret
  author: Faique
  severity: CRITICAL
  tags: token,file,github


requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        regex:
          - '\b(EAACEdEose0cBA[a-zA-Z0-9]+)\b'